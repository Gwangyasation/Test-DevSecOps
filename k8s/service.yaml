apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: ClusterIP      # ← เปลี่ยนจาก NodePort เป็น ClusterIP
  selector:
    app: myapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
